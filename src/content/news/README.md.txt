Short news items: 1-3 sentences plus a link. Frontmatter: title, date, url, draft.
These have no page of their own; they render inline in the /blog/ stream.

The Phase 4 Action adds these with draft: true via a PR, per decision 4b — a human
merges. Nothing here is ever auto-published.
